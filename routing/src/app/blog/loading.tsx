// NOTE: This is shown when we navigate to a page and the page is being loaded(i.e fetch/axios calls or promises are being resolved and page is rendered)
// Just go from Home page to BLog page and this should show momentarily

const Loading = async () => {
  return (
    <div>Loading. Just hol' up Lol !!!</div>
  )
}

export default Loading